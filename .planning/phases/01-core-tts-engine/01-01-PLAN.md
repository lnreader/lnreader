---
phase: 01-core-tts-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/tts/engines/TTSEngine.ts
  - src/tts/engines/NativeTTSEngine.ts
  - src/tts/engines/OfflineTTSEngine.ts
  - src/tts/models/VoiceModel.ts
  - src/tts/models/VoiceMetadata.ts
autonomous: true
requirements:
  - TTS-01
  - TTS-08
must_haves:
  truths:
    - 'User can toggle between native iOS TTS and offline Piper TTS'
    - 'Both engines share unified play/pause/stop interface'
    - 'Offline TTS falls back to native on initialization failure'
  artifacts:
    - path: 'src/tts/engines/TTSEngine.ts'
      provides: 'Abstract TTS engine interface (Strategy Pattern)'
      exports: ['TTSEngine interface', 'EngineType enum']
    - path: 'src/tts/engines/NativeTTSEngine.ts'
      provides: 'expo-speech implementation'
      exports: ['NativeTTSEngine class']
    - path: 'src/tts/engines/OfflineTTSEngine.ts'
      provides: 'Sherpa-ONNX offline TTS implementation'
      exports: ['OfflineTTSEngine class']
  key_links:
    - from: 'src/tts/PlayerController.ts'
      to: 'src/tts/engines/TTSEngine.ts'
      via: 'implements TTSEngine interface'
      pattern: 'implements TTSEngine'
---

<objective>
Install TTS dependencies and create TTS Engine abstraction layer using Strategy Pattern
</objective>

Purpose: Establish foundation for unified play/pause/stop controls across both native iOS TTS and offline Piper TTS engines

Output: TTSEngine interface with NativeTTSEngine and OfflineTTSEngine implementations

<tasks>

<task type="auto">
  <name>task 1: Install TTS dependencies</name>
  <files>package.json</files>
  <action>
    Install required npm packages:
    - react-native-sherpa-onnx-offline-tts@0.2.6
    - react-native-fs@^2.20.0
    - react-native-zip-archive@^6.0.0
    - @react-native-async-storage/async-storage@^2.0.0

    Then run: cd ios && pod install

  </action>
  <verify>
    <automated>npm list react-native-sherpa-onnx-offline-tts react-native-fs react-native-zip-archive @react-native-async-storage/async-storage</automated>
  </verify>
  <done>All 4 packages installed with correct versions</done>
</task>

<task type="auto">
  <name>task 2: Create TTS Engine abstraction interface</name>
  <files>src/tts/engines/TTSEngine.ts</files>
  <action>
    Create abstract TTSEngine interface with Strategy Pattern:
    - EngineType enum (native, offline)
    - TTSEngine interface with methods:
      - initialize(): Promise<void>
      - speak(text: string): Promise<void>
      - pause(): void
      - resume(): void
      - stop(): void
      - setVoice(voiceId: string): void
      - setSpeed(speed: number): void
      - isInitialized(): boolean
      - isSpeaking(): boolean
      - getAvailableVoices(): VoiceMetadata[]

    Create VoiceMetadata type in src/tts/models/VoiceMetadata.ts

  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>TTSEngine interface and VoiceMetadata type defined and exported</done>
</task>

<task type="auto">
  <name>task 3: Implement NativeTTSEngine</name>
  <files>src/tts/engines/NativeTTSEngine.ts</files>
  <action>
    Create NativeTTSEngine class implementing TTSEngine interface using expo-speech:
    - Use Speech.speak() for speak()
    - Use Speech.pause() for pause()
    - Use Speech.stop() for stop()
    - Use Speech.getAvailableVoicesAsync() for getAvailableVoices()
    - Store voice ID and speed in instance variables
    - Handle isSpeaking state tracking
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>NativeTTSEngine fully implements TTSEngine interface using expo-speech</done>
</task>

<task type="auto">
  <name>task 4: Implement OfflineTTSEngine</name>
  <files>src/tts/engines/OfflineTTSEngine.ts</files>
  <action>
    Create OfflineTTSEngine class implementing TTSEngine interface using react-native-sherpa-onnx-offline-tts:
    - Use TTSManager.initialize() with config in initialize()
    - Use TTSManager.generateAndPlay() for speak() with speakerId=0 and speed param
    - Use TTSManager.stop() for stop()
    - Implement pause/resume via stop + re-speak from saved position
    - Implement getAvailableVoices() from local model storage
    - Handle initialization errors gracefully (throw for fallback)
    
    IMPORTANT: Absolute paths required for model files - use RNFS.DocumentDirectoryPath
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>OfflineTTSEngine implements TTSEngine interface using Sherpa-ONNX</done>
</task>

</tasks>

<success_criteria>
Both NativeTTSEngine and OfflineTTSEngine implement TTSEngine interface, enabling runtime engine switching via Strategy Pattern
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-tts-engine/01-01-SUMMARY.md`
</output>
