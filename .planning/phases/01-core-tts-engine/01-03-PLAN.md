---
phase: 01-core-tts-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/screens/settings/VoiceSelectionScreen.tsx
  - src/components/TTSPlayerBar.tsx
  - src/screens/settings/SettingsScreen.tsx
  - src/tts/hooks/useTTS.ts
autonomous: true
requirements:
  - TTS-01
  - TTS-07
  - TTS-08
must_haves:
  truths:
    - 'User can view list of available voices with download status'
    - 'User can tap voice to download and select'
    - 'User can preview voice before downloading'
    - 'User can toggle between native and offline TTS in settings'
    - 'TTS playback controls visible in app'
  artifacts:
    - path: 'src/screens/settings/VoiceSelectionScreen.tsx'
      provides: 'Voice selection UI with download management'
      exports: ['VoiceSelectionScreen component']
    - path: 'src/components/TTSPlayerBar.tsx'
      provides: 'TTS playback controls bar'
      exports: ['TTSPlayerBar component']
    - path: 'src/tts/hooks/useTTS.ts'
      provides: 'React hook for TTS functionality'
      exports: ['useTTS hook']
  key_links:
    - from: 'src/screens/settings/VoiceSelectionScreen.tsx'
      to: 'src/tts/manager/ModelManager.ts'
      via: 'downloads voices on tap'
      pattern: 'modelManager.downloadModel'
    - from: 'src/components/TTSPlayerBar.tsx'
      to: 'src/tts/PlayerController.ts'
      via: 'useTTS hook'
      pattern: "useTTS\\(\\)"
---

<objective>
Create Voice Selection UI and TTS playback controls
</objective>

Purpose: Enable users to select voices, download on-demand, and control playback from app UI

Output: VoiceSelectionScreen, TTSPlayerBar, and useTTS hook

<tasks>

<task type="auto">
  <name>task 1: Create useTTS hook</name>
  <files>src/tts/hooks/useTTS.ts</files>
  <action>
    Create React hook for TTS functionality:
    - Initialize PlayerController on mount
    - Expose: play, pause, stop, setSpeed, setVoice, isPlaying, isPaused, currentEngine
    - Auto-initialize offline TTS engine on first use
    - Handle fallback to native TTS on offline failure
    - Persist user preferences (selected engine, voice, speed) to AsyncStorage
    
    Store preferences under keys: 'tts_engine', 'tts_voice', 'tts_speed'
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>useTTS hook provides reactive TTS controls</done>
</task>

<task type="auto">
  <name>task 2: Create VoiceSelectionScreen</name>
  <files>src/screens/settings/VoiceSelectionScreen.tsx</files>
  <action>
    Create voice selection screen:
    - Header with "Voice Selection" title and back button
    - Section: "Offline Voices" - list of Piper voices
      - Each item shows: voice name, language, download status icon
      - Tap on undownloaded voice: start download with progress indicator
      - Tap on downloaded voice: select as active voice
      - Long press: show delete option for downloaded voices
    - Section: "Preview" - tap to hear sample of selected voice
    - Use existing app components/styles from src/screens/settings/
    
    Downloaded state stored by ModelManager, selected state by VoiceManager
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>VoiceSelectionScreen shows all voices with download/select functionality</done>
</task>

<task type="auto">
  <name>task 3: Create TTSPlayerBar component</name>
  <files>src/components/TTSPlayerBar.tsx</files>
  <action>
    Create TTS playback control bar component:
    - Fixed position at bottom of screen when TTS is active
    - Shows: current voice name, play/pause button, stop button
    - Tap play/pause: toggle playback
    - Tap stop: stop playback and hide bar
    - Animated slide up/down on show/hide
    
    Uses useTTS hook for playback control
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>TTSPlayerBar displays playback controls</done>
</task>

<task type="auto">
  <name>task 4: Add TTS settings to SettingsScreen</name>
  <files>src/screens/settings/SettingsScreen.tsx</files>
  <action>
    Add TTS-related settings to existing SettingsScreen:
    - Add "Text-to-Speech" section header
    - Add row: "Voice" - navigate to VoiceSelectionScreen on tap, shows selected voice name
    - Add row: "Engine" - toggle between "Native" and "Offline (Piper)" with switch
    - Add row: "Speed" - shows current speed (0.5x - 2.0x)
    
    Use existing SettingsScreen styles and component patterns
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>TTS settings integrated into Settings screen with engine toggle</done>
</task>

</tasks>

<verification>
User can select voice, download on tap, toggle engine, and control playback from UI
</verification>

<success_criteria>
Complete TTS feature with voice selection, download management, and playback controls
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-tts-engine/01-03-SUMMARY.md`
</output>
