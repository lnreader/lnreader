---
phase: 01-core-tts-engine
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/tts/manager/ModelManager.ts
  - src/tts/manager/VoiceManager.ts
  - src/tts/PlayerController.ts
  - src/tts/index.ts
autonomous: true
requirements:
  - TTS-01
  - TTS-07
must_haves:
  truths:
    - 'User can download, extract, and delete voice models'
    - 'Voice models stored in app Documents directory'
    - 'Model integrity verified after download'
    - 'PlayerController provides unified play/pause/stop API'
  artifacts:
    - path: 'src/tts/manager/ModelManager.ts'
      provides: 'Voice model lifecycle management'
      exports: ['ModelManager class']
    - path: 'src/tts/manager/VoiceManager.ts'
      provides: 'Voice enumeration and selection'
      exports: ['VoiceManager class']
    - path: 'src/tts/PlayerController.ts'
      provides: 'Unified playback controls'
      exports: ['PlayerController class']
  key_links:
    - from: 'src/tts/PlayerController.ts'
      to: 'src/tts/engines/TTSEngine.ts'
      via: 'uses TTSEngine interface'
      pattern: 'private engine: TTSEngine'
---

<objective>
Create Model Manager for voice downloads and PlayerController for unified playback
</objective>

Purpose: Enable on-demand voice model downloads with progress tracking, and provide unified play/pause/stop controls

Output: ModelManager, VoiceManager, and PlayerController classes

<tasks>

<task type="auto">
  <name>task 1: Create ModelManager</name>
  <files>src/tts/manager/ModelManager.ts</files>
  <action>
    Create ModelManager class for voice model lifecycle:
    - Constructor takes base URL for voice models (GitHub releases)
    - downloadModel(modelId, onProgress): Download ZIP from URL, extract to Documents/voices/{modelId}
    - deleteModel(modelId): Remove extracted model files
    - verifyModel(modelId): Check required files exist (model.onnx, tokens.txt)
    - getModelPath(modelId): Return absolute path to extracted model
    - isModelDownloaded(modelId): Check if model exists and verified
    - getDownloadedModels(): List all downloaded model IDs
    
    Use react-native-fs for file operations and react-native-zip-archive for extraction.
    Model files stored at: {RNFS.DocumentDirectoryPath}/voices/{modelId}/
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>ModelManager handles download/extract/delete/verify operations</done>
</task>

<task type="auto">
  <name>task 2: Create VoiceManager</name>
  <files>src/tts/manager/VoiceManager.ts</files>
  <action>
    Create VoiceManager class for voice enumeration and selection:
    - getAvailableVoices(): Return list of all Piper voices (hardcoded from rhasspy/piper-voices)
    - getDownloadedVoices(): Return only voices that are downloaded (uses ModelManager)
    - selectVoice(voiceId): Set active voice, save to AsyncStorage
    - getSelectedVoice(): Get currently selected voice from storage
    - getVoicePreviewUrl(voiceId): Return URL to preview audio sample
    
    Voice list should include: en_US-ryan-medium, en_US-ryan-low, en_US-lessac-medium, etc.
    Store selected voice ID in AsyncStorage under key 'tts_selected_voice'
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>VoiceManager provides voice enumeration and selection API</done>
</task>

<task type="auto">
  <name>task 3: Create PlayerController</name>
  <files>src/tts/PlayerController.ts</files>
  <action>
    Create PlayerController class with unified playback controls:
    - Constructor takes ModelManager and VoiceManager instances
    - setEngine(engine: TTSEngine): Switch between native/offline TTS
    - initialize(): Initialize current engine, load selected voice
    - play(text: string): Speak text with current voice and speed
    - pause(): Pause current playback
    - resume(): Resume paused playback
    - stop(): Stop and reset playback position
    - setSpeed(speed: number): Set playback speed (0.5-2.0)
    - setVoice(voiceId: string): Switch voice
    - getStatus(): Return { isPlaying, isPaused, currentEngine }
    
    Implement fallback logic: if offline TTS fails to initialize, auto-switch to native
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>PlayerController provides unified play/pause/stop/speed/voice API</done>
</task>

<task type="auto">
  <name>task 4: Create TTS module exports</name>
  <files>src/tts/index.ts</files>
  <action>
    Create src/tts/index.ts with public API exports:
    - Export TTSEngine interface
    - Export EngineType enum
    - Export NativeTTSEngine class
    - Export OfflineTTSEngine class
    - Export ModelManager class
    - Export VoiceManager class
    - Export PlayerController class
    - Create singleton instance getter: getTTSPlayer()
  </action>
  <verify>
    <automated>TypeScript compilation: npx tsc --noEmit</automated>
  </verify>
  <done>Public TTS module API exported from single entry point</done>
</task>

</tasks>

<success_criteria>
ModelManager handles voice downloads with progress, PlayerController provides unified controls
</success_criteria>

<output>
After completion, create `.planning/phases/01-core-tts-engine/01-02-SUMMARY.md`
</output>
